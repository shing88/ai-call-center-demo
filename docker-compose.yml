services:
  ai-call-center-demo:
    build: .
    ports:
      - "4173:4173"
    environment:
      HOST: 0.0.0.0
      PORT: 4173
      HANDOFF_STORE_PATH: /app/data/realtime-handoffs.json
    volumes:
      - ./data:/app/data
