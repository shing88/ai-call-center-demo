{
  "name": "ai-call-center-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build && node scripts/serve-static.mjs dist",
    "build": "tsc -p tsconfig.json && node scripts/copy-static.mjs",
    "test": "tsc -p tsconfig.test.json && node --test .tmp/test/tests/app.test.js",
    "preview": "node scripts/serve-static.mjs dist"
  },
  "devDependencies": {
    "@types/node": "^24.12.4",
    "typescript": "^6.0.3"
  }
}
